{
  "name": "menopause-check",
  "version": "1.4.1",
  "private": true,
  "description": "BSR Menopause Check — self-service service-need triage tool (single-file HTML). This package.json exists only for the Playwright QA harness; the app itself has no build step.",
  "scripts": {
    "test": "node tests/report.test.js",
    "test:local": "node tests/report.test.js --local"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
